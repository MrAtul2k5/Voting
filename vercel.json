{
  "version": 2,
  "builds": [
    {
      "src": "client-project/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "client-project/dist"
      }
    },
    {
      "src": "backend/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/backend/server.js"
    }
  ]
}